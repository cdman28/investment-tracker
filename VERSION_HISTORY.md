# Investment Tracker - 버전 히스토리

## V1.5.1 (2026-06-30)
### UX 개선 — 잔액 입력 체감 수익률 미리보기 & 차트 기간 필터 개선
- **잔액 입력 체감 수익률 실시간 미리보기**: 평가금액 입력 시 체감 수익률을 입력 필드 바로 아래 실시간으로 표시
  - 공식: `(입력금액 − 현재투자원금) ÷ 현재투자원금 × 100`
  - 만원 단위 체크박스 변경 시에도 즉시 재계산
  - 수익은 초록색, 손실은 빨간색으로 표시
- **차트 기간 필터 X축 고정**: 1M/3M/6M/1Y 선택 시 해당 기간 전체가 화면에 표시되도록 개선
  - 기존: 실제 데이터가 있는 날짜 범위만 표시 (데이터 없는 앞부분 잘림)
  - 개선: X축 min = 기간 시작일, max = 오늘로 고정 → 1M 선택 시 항상 최근 1개월 전체가 한 화면에 표시

## V1.5.0 (2026-06-25)
### 수익률 모드 전면 개편 — TWR(정밀) / 체감 수익률
- **"정밀(TWR)" 모드**: 구간별 수익률 체인 곱셈(TWR)으로 교체
  - 입출금 타이밍 영향 없이 순수 운용 성과만 측정
  - `calcTWRFromData` 활용, 연결 잔고 기록이 부족할 경우 단순 수익률로 폴백
  - 전체(All) 탭에서는 브로커별 TWR 가중평균(가중치: 총입금액) 사용
- **"체감" 모드**: `(현재잔고 − 현재투자중인원금) ÷ 현재투자중인원금`으로 교체
  - 출금은 "원금 회수"로 간주, 지금 계좌에 넣어둔 돈 대비 증감만 표시
- **연환산 수익률**: 기존 단순 비례 방식 그대로 유지 (변경 없음)
- **버튼 라벨 변경**: "누적" → "정밀(TWR)", "원금대비" → "체감"
- **오래된 "TWR 사용 안 함" 주석 전면 정리**

## V1.4.2 (2026-06-18)
### 연환산 수익률 계산 방식 개선
- **연환산 수익률 계산 방식 변경**: CAGR(복리 계산)에서 단순 비례식으로 변경
  - 변경 전: `(1 + returnPct/100)^(365/days) - 1` (CAGR)
  - 변경 후: `returnPct × (365/days)` (단순 비례)
  - 더 직관적이고 선형적인 계산 방식으로 통일
- **표시 라벨 변경**: "CAGR" → "단순연환산" (KPI 카드)
- **주석 업데이트**: 코드 주석에서 CAGR 레퍼런스 제거

## V1.4.1 (2026-06-16)
### 입출금 자동 잔액 스냅샷 및 UI 개선
- **입출금 시점 잔액 자동 스냅샷**: 입출금 기록 저장 시 해당 시점의 잔액을 자동으로 기록
  - 메모로 구분: `[AUTO] 입금 시점 잔액` / `[AUTO] 출금 시점 잔액`
  - 입출금 기록(cf_id)과 연결되어 연관 관계 명확
  - 사용자가 별도로 잔액을 입력하지 않아도 스냅샷 생성
- **이후 잔액 자동 보정 기능 제거**: 기존 "이후 잔액 기록 자동 보정" 로직 제거
  - 뒷날짜의 잔액 기록을 일괄 수정하지 않음 (보정이 아닌 스냅샷 방식)
  - 관련 체크박스 UI 제거
- **데스크톱에서 입출금 박스 접기 지원**: 모바일뿐 아니라 데스크톱에서도 입출금 입력 영역 접기/펼치기 가능
- **문구 수정**: "누적 쟁돌" → "누적 수익" (3곳 오타 수정)

## V1.4.0 (2026-06-16)
### 수익률 체계 재정립 및 UI 개선
- **수익률 2가지 분리 표시 (토글)**: KPI 수익률 카드에 "누적 / 원금대비" 토글 버튼 추가
  - **총 누적수익률**: `(현재잔고 + 총출금 - 총입금) / 총입금 × 100` — 출금액 포함 전체 누적 손익
  - **투자원금 대비 수익률**: 동일 공식(총손익 ÷ 총투자원금), 다른 라벨과 설명으로 표시
  - 토글 선택 상태는 localStorage에 저장되어 다음 방문 시 복원
- **TWR를 KPI에서 제거**: 메인 KPI 수익률 표시는 항상 단순 산술 수익률 사용 (TWR 코드는 어노테이션/차트용으로 유지)
- **delta 계산 단순화**: 직전 시점 비교도 TWR 없이 단순 수익률로 통일
- **전고점 수익률 단순화**: TWR 없이 단순 산술 수익률로 표시
- **연환산 수익률 CAGR**: `cumulativeReturnPct` 기준으로 통일
- **입출금 이후 잔액 자동 보정**: 입출금 기록 저장 시 "이후 잔액 기록 자동 보정" 체크박스 추가
  - 체크 시 같은 증권사의 이후 잔액 기록들을 입출금액만큼 자동 증감
  - TWR용 cf_id 연결 기록은 보정 제외
  - 실패 시 메모리 롤백으로 데이터 안전성 보장

## V1.3.0 (2026-06-16)
### 수익률 계산 로직 개선
- **TWR effectiveStart 부호 보정**: 입출금 직후 잔액 기록 방식에 맞게 `effectiveEnd` 역산 방식으로 변경 (입금 직후 잔고에서 CF를 역산하여 직전 잔고 계산)
- **연환산 수익률 CAGR 공식 적용**: 단순 연환산(returnPct × 365/days) → 실제 CAGR 공식 `(1 + r)^(365/days) - 1` 로 변경, 라벨과 공식 일치
- **calcAll TWR 우선**: 전체 탭에서도 TWR 우선 계산 후 단순 수익률 폴백으로 통일
- **delta 가중 방식**: 전체 수익률 delta를 단순 평균 → 잔액 기준 가중평균으로 개선
- **TWR reason 코드 반환**: `calcTWRFromData`가 `{ value, reason }` 객체 반환 (`insufficient_data` / `no_cf_linked` / `no_sub_returns` / `ok`)
- **직전 잔고 → 직후 잔고 방식**: TWR 계산용 잔고 입력 UI를 직후 잔고 방식으로 변경, placeholder/메모 업데이트

### UI 개선
- **입출금 영역 접기/펼치기**: 잔액 기록 입력 / 입출금 기록 입력 섹션을 모바일뿐 아니라 데스크톱에서도 접기/펼치기 가능하도록 개선 (섹션 타이틀 클릭)
- **전고점 수익금 / 전고점 수익률 표시**: KPI 보조 카드에서 "이번 주 변화", "이번 달 변화" 대신 투자 이래 최고 잔고 시점의 수익금/수익률 표시
- **그래프 어노테이션 동그라미 크기 축소**: 차트 이벤트 마커 radius 7→4, hoverRadius 10→6 (약 60% 크기)
- **만원 단위 체크를 수정 창에도 적용**: 잔액 기록/입출금 기록 수정 모달에 만원 단위 체크박스 추가, 글로벌 상태와 연동

## V1.2.2 (2026-06-05)
### 버그 수정
- **'전일대비 변화' 계산 로직 수정**: 정확히 '어제' 기록이 아닌 '가장 최근 기록의 직전 기록'과 비교하도록 변경하여, 매일 기록하지 않아도 값이 올바르게 표시되도록 수정
- **'전일대비 변화' 렌더링 누락 수정**: `setKPI` 함수에서 `dayChange` 값을 화면에 반영하는 코드가 누락된 버그 수정 (항상 '—'로 표시되던 문제 해결)

## V1.2.1 (2026-06-05)
### UI 개선
- **KPI 카드 표시 방식 변경**
  - 기본 수익률 카드에 직전 기록 대비 수익률 변동(▲/▼ %)을 표시
  - 연환산 수익률(CAGR) 카드에서 직전 대비 비율 표시 제거

## V1.2.0 (2026-06-02)
### 기능 추가
- **다중 기기 동시 접속 충돌 방지** (PC + 폰 동시 사용 시 데이터 손실 문제 해결)
  - **Read-before-write 병합**: 저장 전 서버 최신 데이터를 가져와 자동 병합
    - 로컬에서 추가한 레코드 → 병합 결과에 포함
    - 로컬에서 삭제한 레코드 → 병합 결과에서 제외
    - 로컬에서 수정한 레코드 → 로컬 버전 우선 적용
    - 다른 기기에서 추가한 레코드 → 자동 병합하여 보존
  - **자동 새로고침 (3분 룰)**: 탭/앱이 3분 이상 백그라운드 상태 후 복귀 시 자동으로 최신 데이터 로드
  - 병합/자동 업데이트 시 토스트 메시지로 사용자에게 알림

## V1.1.1 (2026-06-02)
### 버그 수정
- **타임존 KST 고정**: 시스템 로컬 타임존이 KST가 아닌 환경에서도 날짜/시간이 항상 한국 시간(UTC+9)으로 표시·저장되도록 수정
  - `formatDt`: `timeZone: 'Asia/Seoul'` 명시
  - `toLocalDatetimeInput`: UTC+9 오프셋으로 KST 시간 직접 계산
  - `datetimeLocalToISO`: 입력값을 KST(+09:00)로 해석하여 UTC ISO 변환

## V1.1.0 (2025-01-xx)
### 기능 추가
- **설정탭 분리**: 증권사 편집 버튼을 탭바에서 제거하고, 별도 "설정" 탭으로 이동
  - 설정탭에서 증권사 이름/색상/목표수익률 수정 및 삭제 가능
  - 실수로 편집 버튼을 누르는 불편함 해소
- **모바일 섹션 접기**: 모바일 화면(767px 이하)에서 아래 3개 섹션 접기/펼치기 지원
  - 자산 차트
  - 잔액 기록 입력
  - 입출금 기록 입력
  - 접힘 상태를 localStorage에 저장하여 다음 방문 시 기억

## V1.0.0 (초기 버전)
### 기능
- GitHub Gist 연동 데이터 저장
- 증권사별 잔액/입출금 기록
- 수익률 계산 (TWRR 방식)
- 라인 차트 / 파이 차트
- 다크모드 지원
- 만원 단위 표시 옵션
