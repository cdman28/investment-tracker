# 📊 투자 기록 웹앱 — 테크스펙 v1.1

> **프로젝트명**: Investment Tracker  
> **작성일**: 2026-05-07  
> **버전**: v1.1 (미결 사항 반영 완료)  
> **상태**: 테크스펙 확정 ✅

---

## 1. 프로젝트 개요

### 목적
- PC와 모바일 모두에서 접근 가능한 **웹 기반 주식 투자 기록 관리 앱**
- 개별 종목 추적 없이 **증권사별 계좌 전체 평가금액** 단위로 투자 성과를 기록·추적
- 개인정보 없이 순수 투자 데이터만 관리
- **혼자 사용**하는 개인 도구

### 핵심 원칙
- 입력은 단순하게: **현재 전체 평가금액 1개**만 입력
- 입출금 기록으로 **원금을 별도 관리**해 정확한 수익률 산출
- 출금 후에도 **누적 투자원금 기반 수익률** 유지
- 증권사 간 이체는 별도 기능 없이 **수동 입출금 기록으로 처리**

---

## 2. 기술 스택

### Frontend
| 항목 | 기술 | 비고 |
|------|------|------|
| 마크업 | HTML5 | 시맨틱 구조 |
| 스타일 | CSS3 (CSS Variables + Flexbox/Grid) | 디자인 토큰 기반 |
| 스크립트 | Vanilla JavaScript (ES2022+) | 프레임워크 없음 |
| 차트 | Chart.js (CDN) | 라인차트, 파이차트 |
| 아이콘 | Lucide Icons (CDN) | SVG 기반 |
| 폰트 | Pretendard (CDN) | 한국어 최적화 |

### Backend / 데이터 저장: **GitHub Gist**

#### 선택 이유
- 혼자 사용하는 개인 도구 → 동시 접근 충돌 가능성 없음
- GitHub 계정만 있으면 별도 서비스 가입 불필요
- 완전 무료, 용량 제한 사실상 없음
- Personal Access Token(PAT) 1개로 읽기/쓰기 가능
- 데이터가 GitHub에 JSON 파일로 보관 → 수동 백업/확인도 가능

#### 동작 방식
```
[브라우저 앱]
    ↕ GitHub REST API (HTTPS)
[GitHub Gist] — JSON 파일 1개에 전체 데이터 저장
```

- 앱 최초 실행 시 설정 화면에서 **GitHub PAT + Gist ID** 입력
- 입력값은 브라우저 **sessionStorage**에만 임시 보관 (탭 닫으면 초기화)
- 앱 접속할 때마다 PAT + Gist ID 입력 → 보안성 유지
- 모든 읽기/쓰기는 `https://api.github.com/gists/{gist_id}` API 사용

#### GitHub PAT 권한 설정
- 필요 권한: `gist` 스코프만 체크 (최소 권한 원칙)
- PAT 유형: Fine-grained token 또는 Classic token 모두 가능

#### Gist 데이터 파일 구조
```
Gist 파일명: investment-tracker-data.json
```

---

## 3. 데이터 구조

### 전체 JSON 구조 (Gist에 저장되는 형태)

```json
{
  "version": "1.0",
  "last_updated": "2026-05-07T17:00:00Z",
  "brokers": [...],
  "balance_records": [...],
  "cashflow_records": [...]
}
```

### 3-1. 증권사 (Brokers)

```json
{
  "id": "uuid",
  "name": "토스증권",
  "color": "#3182F6",
  "target_return": 15.0,
  "created_at": "2026-01-01T00:00:00Z",
  "order": 1
}
```

| 필드 | 타입 | 설명 |
|------|------|------|
| `id` | string (UUID) | 고유 식별자 |
| `name` | string | 증권사명 (예: 토스증권) |
| `color` | string | 탭 구분용 색상 (hex) |
| `target_return` | number | **목표 수익률 (%)** — 증권사별 개별 설정 |
| `created_at` | string (ISO 8601) | 생성 시각 |
| `order` | integer | 탭 표시 순서 |

> **목표 수익률 (`target_return`)**: 각 증권사 탭의 라인차트에 수평 참고선으로 표시. 전체 탭에서는 각 증권사의 목표수익률을 범례로 함께 표시.

---

### 3-2. 잔액 기록 (Balance Records)

**현재 계좌 평가금액을 입력할 때마다 생성되는 스냅샷**

```json
{
  "id": "uuid",
  "broker_id": "uuid",
  "balance": 1100000,
  "recorded_at": "2026-05-07T17:00:00Z",
  "memo": "5월 첫째 주 기록",
  "created_at": "2026-05-07T17:00:00Z"
}
```

| 필드 | 타입 | 설명 |
|------|------|------|
| `id` | string (UUID) | 고유 식별자 |
| `broker_id` | string (UUID) | 소속 증권사 ID |
| `balance` | integer | 현재 전체 평가금액 (원 단위) |
| `recorded_at` | string (ISO 8601) | 기록 기준 시각 **(수정 가능)** |
| `memo` | string? | 선택 메모 (수정 가능) |
| `created_at` | string (ISO 8601) | 실제 입력 시각 (수정 불가, 감사 목적) |

---

### 3-3. 입출금 기록 (Cashflow Records)

**원금 추적 및 수익률 계산을 위한 입금/출금 이력**

```json
{
  "id": "uuid",
  "broker_id": "uuid",
  "type": "deposit",
  "amount": 1000000,
  "transacted_at": "2026-01-15T09:00:00Z",
  "memo": "초기 입금",
  "created_at": "2026-01-15T09:00:00Z"
}
```

| 필드 | 타입 | 설명 |
|------|------|------|
| `id` | string (UUID) | 고유 식별자 |
| `broker_id` | string (UUID) | 소속 증권사 ID |
| `type` | enum | `deposit` (입금) / `withdrawal` (출금) |
| `amount` | integer | 금액 (원 단위, 항상 양수) |
| `transacted_at` | string (ISO 8601) | 입출금 기준 시각 **(수정 가능)** |
| `memo` | string? | 선택 메모 (수정 가능) |
| `created_at` | string (ISO 8601) | 실제 입력 시각 (수정 불가, 감사 목적) |

> **증권사 간 이체 처리 방법**: 별도 이체 기능 없음. A증권 → B증권 이체 시 A에 출금 기록, B에 입금 기록을 각각 수동 입력.

---

## 4. 수익률 계산 로직

### 핵심 공식

출금을 해도 수익은 누적 반영. 출금액은 "이미 실현한 금액"으로 처리하고, **원금 기준은 총 입금액으로 고정**.

```
누적 입금액   = SUM( type='deposit'    인 amount )
누적 출금액   = SUM( type='withdrawal' 인 amount )
현재 평가금액 = 해당 증권사의 가장 최근 balance 기록값

총 손익금액   = 현재 평가금액 + 누적 출금액 − 누적 입금액
총 수익률(%)  = 총 손익금액 ÷ 누적 입금액 × 100
```

### 예시 시뮬레이션

| 시점 | 액션 | 평가금액 | 누적 입금 | 누적 출금 | 총 손익 | 수익률 |
|------|------|---------|---------|---------|--------|--------|
| 1월 | 100만원 입금 | 100만원 | 100만원 | 0 | 0원 | 0.00% |
| 3월 | (주가 상승, 잔액 기록) | 110만원 | 100만원 | 0 | +10만원 | **+10.00%** |
| 4월 | 10만원 출금 후 잔액 기록 | 100만원 | 100만원 | 10만원 | +10만원 | **+10.00%** ✅ |
| 6월 | (주가 상승, 잔액 기록) | 105만원 | 100만원 | 10만원 | +15만원 | **+15.00%** |
| 7월 | 추가 50만원 입금 | 105만원 | 150만원 | 10만원 | −35만원 | **−23.33%** |

### 전체 탭 합산 방식
```
전체 누적 입금   = SUM(모든 증권사의 누적 입금액)
전체 누적 출금   = SUM(모든 증권사의 누적 출금액)
전체 현재 평가   = SUM(모든 증권사의 최근 평가금액)

전체 총 손익     = 전체 현재 평가 + 전체 누적 출금 − 전체 누적 입금
전체 수익률(%)   = 전체 총 손익 ÷ 전체 누적 입금 × 100
```

---

## 5. 화면 구성 (UI Layout)

### 전체 레이아웃
```
┌─────────────────────────────────────────────┐
│  [로고] Investment Tracker      [🌙다크모드]  │  ← 헤더
├─────────────────────────────────────────────┤
│  [전체] [토스증권] [카카오페이] [+ 증권사 추가] │  ← 탭바
├─────────────────────────────────────────────┤
│  [총투자금]  [평가금액]  [수익금]  [수익률]    │  ← KPI 카드 4개
│  (보조) [MDD]  [이번주 변화]  [이번달 변화]   │  ← 보조 KPI
├──────────────────────┬──────────────────────┤
│   자산 추이 차트        │  포트폴리오 비중       │
│   (라인차트)           │  (파이차트)           │
│   [1M][3M][6M][1Y][ALL] │  (전체탭에서만 표시)  │
│   — 목표수익률 라인 —   │                     │
├──────────────────────┴──────────────────────┤
│  ▼ 잔액 기록 입력                            │
│  [현재 평가금액 ________] [메모 ________] [저장] │
├─────────────────────────────────────────────┤
│  ▼ 입출금 기록 입력                          │
│  [● 입금  ○ 출금]  [금액 _____]             │
│  [날짜/시간 _____]  [메모 _____]  [저장]     │
├─────────────────────────────────────────────┤
│  ▼ 기록 목록  [잔액기록 | 입출금기록]  탭 전환  │
│  날짜시간 | 구분 | 금액 | 수익률 | 메모 | [✏️][🗑️] │
├─────────────────────────────────────────────┤
│  [📥CSV 내보내기]  [💾JSON 백업]  [📤JSON 복원] │  ← 데이터 관리
└─────────────────────────────────────────────┘
```

### 반응형 기준
| 뷰포트 | 레이아웃 변화 |
|--------|-------------|
| 375px~ (모바일) | 1컬럼, KPI 2×2 그리드, 차트 풀너비 세로 배치, 파이차트 하단 |
| 768px~ (태블릿) | KPI 4개 1행, 차트 2컬럼 나란히 |
| 1024px~ (PC) | 여유 있는 2컬럼, 기록 목록 테이블 확장 |

---

## 6. 기능 명세

### 6-1. 초기 설정 화면 (앱 최초 진입 시)
- GitHub Personal Access Token 입력 필드
- Gist ID 입력 필드
- [연결 확인] 버튼 → API 호출 성공/실패 메시지 표시
- 입력값은 **sessionStorage에만 저장** (탭/브라우저 닫으면 초기화)
- 새 Gist 생성 도우미 링크 제공 (`https://gist.github.com/` 바로가기)

### 6-2. 탭 관리
| 기능 | 상세 |
|------|------|
| 전체 탭 | 항상 첫 번째 고정, 삭제 불가 |
| 증권사 탭 추가 | 이름 + 대표 색상 선택 + 목표 수익률(%) 입력 |
| 증권사 탭 설정 수정 | 이름, 색상, 목표 수익률 수정 가능 |
| 증권사 탭 삭제 | 해당 증권사의 모든 기록 함께 삭제, 확인 모달 필수 |

### 6-3. 잔액 기록
| 기능 | 상세 |
|------|------|
| 입력 | 현재 평가금액(숫자) + 선택 메모 |
| 자동 기록 | 저장 시 `recorded_at` = 현재 날짜/시간 자동 설정 |
| 수정 | 평가금액, 기준 날짜/시간, 메모 모두 수정 가능 |
| 삭제 | 개별 삭제 가능 |

### 6-4. 입출금 기록
| 기능 | 상세 |
|------|------|
| 입력 | 타입(입금/출금) + 금액 + 날짜(기본: 오늘) + 메모 |
| 수정 | 타입, 금액, 날짜/시간, 메모 모두 수정 가능 |
| 삭제 | 개별 삭제 가능 |
| 수익률 반영 | 저장/수정/삭제 시 KPI 즉시 재계산 |

### 6-5. KPI 카드

| 카드 | 계산 방법 |
|------|---------|
| 총 투자금액 | 누적 입금 합계 |
| 현재 평가금액 | 가장 최근 잔액 기록값 |
| 총 손익금액 | 현재 평가 + 누적 출금 − 누적 입금 |
| 수익률 | 총 손익 ÷ 누적 입금 × 100 (%) |
| MDD (보조) | (최고 평가금액 − 최저 평가금액) ÷ 최고 평가금액 × 100 |
| 이번 주 변화 (보조) | 이번 주 첫 기록 대비 최근 기록 차이 |
| 이번 달 변화 (보조) | 이번 달 첫 기록 대비 최근 기록 차이 |

- 수익 → `--color-success` (초록), 손실 → `--color-error` (빨강), 보합 → 회색
- 숫자 변화 시 카운팅 애니메이션 적용

### 6-6. 차트

| 차트 | 상세 |
|------|------|
| 자산 추이 (라인차트) | X축: 날짜, Y축: 평가금액. 기간 필터: 1M · 3M · 6M · 1Y · ALL |
| 목표 수익률 라인 | 라인차트에 수평 참고선으로 표시. 증권사별 `target_return` 값 사용 |
| 포트폴리오 비중 (파이차트) | **전체 탭에서만** 표시. 증권사별 현재 평가금액 비중 |

- 전체 탭의 라인차트: 증권사별 선을 **다른 색으로** 겹쳐서 표시
- 전체 탭의 목표수익률 라인: 각 증권사별 목표수익률을 각자 색상의 점선으로 표시

### 6-7. 데이터 관리

| 기능 | 상세 |
|------|------|
| CSV 내보내기 | 현재 탭의 잔액 기록 + 입출금 기록을 `.csv` 파일로 다운로드 |
| JSON 백업 | 전체 앱 데이터 (`brokers` + 모든 기록)를 `.json` 파일로 내보내기 |
| JSON 복원 | JSON 파일 업로드 → 기존 데이터 **덮어쓰기 경고 모달** → 확인 시 복원 |

---

## 7. GitHub Gist 연동 상세

### API 사용 방법

| 작업 | HTTP 메서드 | 엔드포인트 |
|------|------------|----------|
| 데이터 읽기 | `GET` | `https://api.github.com/gists/{gist_id}` |
| 데이터 쓰기 | `PATCH` | `https://api.github.com/gists/{gist_id}` |

### 요청 헤더 (공통)
```
Authorization: Bearer {GITHUB_PAT}
Accept: application/vnd.github+json
X-GitHub-Api-Version: 2022-11-28
Content-Type: application/json
```

### 쓰기 요청 Body 예시
```json
{
  "files": {
    "investment-tracker-data.json": {
      "content": "{...전체 JSON 데이터...}"
    }
  }
}
```

### 데이터 동기화 전략
- 앱 진입 시: Gist에서 전체 데이터 1회 로드
- 기록 추가/수정/삭제 시: 메모리 내 데이터 수정 → 즉시 Gist에 PATCH 저장
- 저장 중 표시: 저장 버튼에 로딩 스피너 표시
- 저장 실패 시: 오류 메시지 + 재시도 버튼 표시

### 초기 Gist 파일 생성 절차 (사용자 가이드)
1. `https://gist.github.com/` 접속
2. 파일명: `investment-tracker-data.json`
3. 내용: `{}` 입력 후 **Create secret gist** 클릭 (비공개 권장)
4. 생성된 Gist의 URL에서 ID 복사 (`gist.github.com/{username}/{gist_id}`)
5. 앱 설정 화면에 PAT + Gist ID 입력

---

## 8. 디자인 시스템

### 색상
- 기반: Nexus 디자인 시스템 (따뜻한 베이지 배경 + 틸 액센트)
- 수익: `--color-success` (그린)
- 손실: `--color-error` (레드)
- 증권사 구분색: 사용자 지정, 기본 팔레트 7색 제공

### 폰트
- 본문: **Pretendard** (한국어 최적화 고딕)
- 숫자: `font-variant-numeric: tabular-nums lining-nums` (열 정렬)

### 다크/라이트 모드
- 시스템 설정 자동 감지 (`prefers-color-scheme`)
- 수동 토글 버튼 (헤더 우측)
- 선택값은 **sessionStorage에만 유지** (localStorage 미사용)

---

## 9. 비기능 요구사항

| 항목 | 기준 |
|------|------|
| 반응형 | 375px ~ 2560px 전 해상도 지원 |
| 접근성 | WCAG AA 기준 (대비 4.5:1, 키보드 네비게이션, aria-label) |
| 성능 | 초기 로드 1.5MB 이하, 차트 렌더링 300ms 이하 |
| 보안 | PAT는 sessionStorage에만 보관, 앱 코드에 하드코딩 금지 |
| 오프라인 | GitHub API 연결 실패 시 명확한 오류 메시지 표시 |
| 단일 파일 | 배포 파일은 HTML 1개 (CSS·JS 인라인 포함) |

---

## 10. 개발 단계 (구현 로드맵)

| Phase | 내용 |
|-------|------|
| 1 | HTML 구조 + CSS 디자인 시스템 + 다크/라이트 모드 |
| 2 | 초기 설정 화면 (PAT + Gist ID 입력 + 연결 확인) |
| 3 | GitHub Gist 읽기/쓰기 모듈 구현 |
| 4 | 탭 구조 + 증권사 추가/수정/삭제 |
| 5 | 잔액 기록 입력 + 수정 + 삭제 |
| 6 | 입출금 기록 + 수익률 계산 로직 |
| 7 | KPI 카드 + 보조 지표 (MDD, 주간/월간 변화) |
| 8 | 차트 (라인차트 + 파이차트 + 목표수익률 라인) |
| 9 | 기간 필터 (1M·3M·6M·1Y·ALL) |
| 10 | CSV/JSON 내보내기·복원 |
| 11 | 반응형 최적화 (모바일 375px 기준) |
| 12 | 전체 QA + 버그 수정 |

---

## 11. 미결 사항

> v1.1 기준 모든 미결 사항 해소 완료 ✅

| 항목 | 결정 내용 |
|------|---------|
| DB 백엔드 | GitHub Gist (PAT + Gist ID 방식) |
| 인증 방식 | API 키(PAT) 직접 입력, sessionStorage 임시 보관 |
| 증권사 간 이체 | 별도 기능 없음 — 수동 입출금 기록으로 처리 |
| 목표 수익률 | 증권사별 개별 설정 (`target_return` 필드) |

---

*버전 히스토리*

| 버전 | 날짜 | 변경 내용 |
|------|------|---------|
| v1.0 | 2026-05-07 | 초안 작성 |
| v1.1 | 2026-05-07 | DB → GitHub Gist 확정, 목표수익률 증권사별 설정, 이체 기능 제거, Gist API 상세 추가 |
